{% extends 'base.html.twig' %}

{% block submenulist %}
    {% set d_links = {
    'Product List':'yaml_product_list',
    'Product':'yaml_product',
    }
    %}
        <ul class="nav navbar-nav navbar-left">
            {% for key,link in d_links %}
                <li><a href="{{ path(link) }}">{{ key }}</a></li><br>
            {% endfor %}

        </ul>
{% endblock %}

{% block body %}
<h3>Product list</h3>
{% endblock%}